
/- Remove the placeholder and complete the following proofs -/
open classical

variables A B C : Prop
variables p q r : Prop

-- 1
example : A ∨ B → B ∨ A := _

-- 2
example : A ∧ (B ∨ C) → (A ∧ B) ∨ (A ∧ C) := _

-- 3
example : ¬(A ∨ B) ↔ ¬A ∧ ¬B := _

-- 4
example : ((p → q) ∧ (¬r → ¬q)) → (p → r) := _




